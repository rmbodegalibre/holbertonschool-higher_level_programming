#!/usr/bin/node
// This script contains a function that converts a number from base 10 to another base passed as argument:
// No files are imported in this script
// No new variebles are declared in this script (var, let, etc)
exports.converter = function (base) {
  return converted => converted.toString(base);
};

#!/usr/bin/node
// This script updates the text color of the <header> element to red (#FF0000):
// It uses document.querySelector to select the HTML tag
// It doesn't the JQuery API
document.querySelector('HEADER').style.color = '#FF0000';
